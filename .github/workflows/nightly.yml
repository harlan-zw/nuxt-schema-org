name: Nightly

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
    tags:
      - '!**'

permissions:
  contents: read

jobs:
  build:
    uses: harlan-zw/nuxt-seo/.github/workflows/reusable-nightly.yml@main
